{
  "name": "jegvet",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual veterinary reference and dosing toolkit (static web app).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/search-engine.test.js",
    "test:browser": "playwright test",
    "test:all": "node --test && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  },
  "license": "UNLICENSED"
}
